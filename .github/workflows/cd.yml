name: Docker Image CI

on:
  push:
    branches: [ "base" ]
  pull_request:
    branches: [ "base" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: docker login
      env: 
        DOCKER_USER: acnaweb
        DOCKER_PASSWORD: xpto
      run: |
        docker login -u $DOCKER_USER -p $DOCKER_PASSWORD